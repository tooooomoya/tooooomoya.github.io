# ローカルプレビュー手順

このサイトは静的HTMLなので、ローカルでHTTPサーバーを立てれば確認できます。

## 起動

プロジェクトのルート（このファイルがある場所）で:

```bash
python3 -m http.server 8000
```

## アクセス

ブラウザで以下を開く:

- トップページ: http://localhost:8000/
- 旅行ページ: http://localhost:8000/trip-XXXXXXXX/  （※実際のIDは身内だけで共有）

HTML / CSS / JS を編集したら、ブラウザを再読み込みするだけで反映されます。

## 停止

サーバーを起動したターミナルで `Ctrl + C`。

ポート8000を使うプロセスが残ってしまった場合:

```bash
pkill -f "http.server 8000"
```

## メモ

- ポート8000が使われていたら、`8000` を別の番号（例 `8080`）に変えればOK。
- `python3` が無い環境では `python -m http.server 8000` でも可。
